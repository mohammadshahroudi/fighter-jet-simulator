using UnityEngine;
using System.Collections;

public class HideOnPlay : MonoBehaviour {

    // Use this for initialization
    void Start () {
        gameObject.SetActive (false); // This script is to be assigned to the mesh
    }
	
    // Update is called once per frame
    void Update () {
	
    }
}